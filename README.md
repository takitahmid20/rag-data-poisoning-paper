# RAG Data Poisoning Paper

IEEE conference-format paper for the RAG data-poisoning project.

## Files

- `main.tex` - IEEEtran manuscript
- `references.bib` - BibTeX references with verified arXiv metadata
- `figures/` - result charts (PDF) used in the paper

## Compile

Install a LaTeX distribution with `IEEEtran.cls`, then run:

```bash
pdflatex main.tex
bibtex main
pdflatex main.tex
pdflatex main.tex
```

Or use `latexmk -pdf main.tex`.

The paper reports two BAAI/bge-m3 evaluations (top-k=3, retrieval-level):

- **10-question gate suite:** 5 trusted + 1 untrusted PDF. Mixed contamination
  40% (4/10), defended (pre-index gate) 0%.
- **Extended multi-attack suite:** 15 trusted + 17 poisoned PDFs (outdated,
  contradiction, authority spoofing, prompt injection, query mirroring), 25
  questions. Poison in top-3: 100% without defense, 68% with a content-only
  scanner (12/17 docs blocked, 0 false positives), 0% with the provenance gate.
  MiniLM-L6-v2 is reported as a comparison model.

Figures in `figures/` are generated by `scripts/make_charts.py` in the source
repository. These are preliminary synthetic results, not a universal security
claim; query mirroring is a heuristic proxy, not an optimized PoisonedRAG attack.

Source implementation:
https://github.com/takitahmid20/rag-data-poisoning
